export function renderInlineViewer(): string {
  return `
    <div id="inline-viewer" class="inline-viewer">
      <div class="inline-viewer-empty">Select a CodeMap from the sidebar to view</div>
    </div>
  `;
}
